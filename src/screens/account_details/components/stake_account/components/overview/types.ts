export type OverviewType = {
  address: string;
  balance: number;
  stakeAuthority: string;
  withdrawAuthority: string;
  rentReserve: number;
  validator: string;
  delegated: number;
  rewards: number;
}
